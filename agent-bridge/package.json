{
  "name": "@solo/agent-bridge",
  "version": "0.1.0",
  "description": "Claude Agent SDK bridge for Solo IDE",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node18 --external bun:sqlite --external @xenova/transformers --external onnxruntime-node --clean --sourcemap",
    "build:bundle": "tsup src/index.ts --format esm --target node18 --external bun:sqlite --external @xenova/transformers --external onnxruntime-node --clean --minify --no-splitting",
    "dev": "tsup src/index.ts --format esm --target node18 --external bun:sqlite --external @xenova/transformers --external onnxruntime-node --watch",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.76",
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@xenova/transformers": "^2.17.2",
    "better-sqlite3": "^12.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20.17.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
