name: Build Master DMG

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-dmg:
    runs-on: macos-latest
    permissions:
      contents: read
    env:
      SOLO_COGNITO_DOMAIN: ${{ vars.SOLO_DEV_COGNITO_DOMAIN }}
      SOLO_COGNITO_CLIENT_ID: ${{ vars.SOLO_DEV_COGNITO_CLIENT_ID }}
      SOLO_AWS_REGION: ${{ vars.SOLO_DEV_AWS_REGION }}
      SOLO_API_ENDPOINT: ${{ vars.SOLO_DEV_API_ENDPOINT }}
      SOLO_VAULT_API_ENDPOINT: ${{ vars.SOLO_DEV_VAULT_API_ENDPOINT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: . -> target

      - name: Install bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install frontend dependencies
        run: bun install

      - name: Generate TypeScript bindings
        run: bun run gen:bindings

      - name: Typecheck frontend
        run: cd apps/desktop && bunx tsc --noEmit

      - name: Validate desktop auth config
        run: |
          test -n "$SOLO_COGNITO_DOMAIN" || {
            echo "Missing GitHub repository variable: SOLO_DEV_COGNITO_DOMAIN" >&2
            exit 1
          }
          test -n "$SOLO_COGNITO_CLIENT_ID" || {
            echo "Missing GitHub repository variable: SOLO_DEV_COGNITO_CLIENT_ID" >&2
            exit 1
          }
          test -n "$SOLO_AWS_REGION" || {
            echo "Missing GitHub repository variable: SOLO_DEV_AWS_REGION" >&2
            exit 1
          }
          test -n "$SOLO_API_ENDPOINT" || {
            echo "Missing GitHub repository variable: SOLO_DEV_API_ENDPOINT" >&2
            exit 1
          }
          test -n "$SOLO_VAULT_API_ENDPOINT" || {
            echo "Missing GitHub repository variable: SOLO_DEV_VAULT_API_ENDPOINT" >&2
            exit 1
          }

      - name: Build DMG
        run: |
          cd apps/desktop
          bunx tauri build \
            --target aarch64-apple-darwin \
            --bundles dmg \
            --config '{"bundle":{"createUpdaterArtifacts":false}}'

      - name: Upload DMG artifact
        uses: actions/upload-artifact@v4
        with:
          name: solo-dmg-${{ github.sha }}
          path: |
            target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
            target/release/bundle/dmg/*.dmg
          if-no-files-found: error
