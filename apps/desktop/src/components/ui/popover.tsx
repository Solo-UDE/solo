// Local shim — re-exports Popover primitives from @solo/ui. Call-site-compatible
// with the prior shadcn wrapper; all existing imports keep working.
export {
  Popover,
  PopoverTrigger,
  PopoverContent,
  PopoverAnchor,
  PopoverClose,
} from "@solo/ui";
