{
  "name": "solo-ide",
  "private": true,
  "workspaces": [
    "apps/desktop",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @solo/desktop dev",
    "build": "bun run --filter @solo/desktop build:vite && cargo build --release",
    "tauri": "bun run --filter @solo/desktop tauri",
    "check": "cargo check && bun run --filter '*' typecheck",
    "test": "cargo test && bun run --filter '*' test",
    "clean": "cargo clean && rm -rf node_modules apps/*/node_modules packages/*/node_modules",
    "gen:bindings": "cargo test -p solo-protocol -p solo-agent && rm -rf apps/desktop/src/bindings && mkdir -p apps/desktop/src/bindings && cp -r crates/solo-protocol/apps/desktop/src/bindings/* apps/desktop/src/bindings/ && cp -r crates/solo-agent/apps/desktop/src/bindings/* apps/desktop/src/bindings/"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "bun@1.1.42"
}
